<%= simple_form_for([ tournament, review ], remote: true, authenticity_token: true) do |form| %>
  <%= form.input :content, input_html: {class: "form-review-chat"} %>
  <%= form.submit "SEND", class: "btn btn-primary btn-primary-chat" %>
<% end %>

