<h1><%= @bar.name %></h1>
  <p>
    <%= @bar.name %>
    <%= @bar.phone_number %>
    <%= @bar.address %>
    <%= @bar.description %>
    <%= @bar.tv_number %>
    <%= @bar.tournament_size %>
    <%= @bar.offers %>
  </p>
