<h1><%= @bar.name %></h1>
  <p>
    <%= @bar.name %>
    <%= @bar.phone_number %>
    <%= @bar.address %>
    <%= @bar.description %>
    <%= @bar.tv_number %>
    <%= @bar.tournament_size %>
    <%= @bar.offers %>
  </p>

<%= link_to "Book this bar and create your tournament!", new_bar_tournament_path(@bar) %>
