<h1><%= @bar.name %></h1>
  <p>
    <%= @bar.name %>
    <%= @bar.phone_number %>
    <%= @bar.address %>
    <%= @bar.description %>
    <%= @bar.tv_number %>
    <%= @bar.tournament_size %>
    <%= @bar.offers %>
    <%= cl_image_tag(@bar.photo, width: 400, height: 300, crop: :fill) %>
  </p>

<%= link_to "Book this bar and create your tournament!", new_bar_tournament_path(@bar) %>
